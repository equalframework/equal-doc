The error handling is widely simplified. 

For example, if I type : 

```
/[localhost]/?get=model_collect

I will get an error code :

"errors": {
        "MISSING_PARAM": "entity"
 }
```

See [Debugging](debugging.md) for more infos.

